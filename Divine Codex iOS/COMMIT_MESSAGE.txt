style(explorer): Simplify and polish ExplorerNodeButton with Liquid Glass

- Renamed component from Node to ExplorerNodeButton (clearer name, avoids potential collisions).
- Simplified button to display only the title (short description reserved for future detail overlay/popup).
- Applied Liquid Glass styling using Capsule + .glassEffect(.clear, in: .capsule), matching the existing TabBar aesthetic.
- Used black fill for unselected state and divine gold for selection.
- Temporarily added a horizontal test row of ExplorerNodeButtons directly in CosmoScene (on black background) for real-time visual evaluation in the running app.
- Cleaned up outdated placeholder comments and test scaffolding.

The button is now lean and ready for integration into the explorer UI and 2D overlays on the 3D scene.

Next: Bind real ExplorerNode data (local + Sanity) and style the full Cosmology Explorer canvas.